# Fintech Credit System - Mobile App

## 📱 How to Run

This application is built with [Flutter](https://flutter.dev/).

### Prerequisites
- Flutter SDK installed
- Android Emulator or Physical Device

### Setup
Dependencies should already be installed. If not:
```bash
flutter pub get
```

### Running the App
To run the application on your connected device or emulator:
```bash
flutter run
```

If you are using an emulator on localhost, the API base URL is set to `http://10.0.2.2:3000`.

## 🛠 Features
- Device Locking Mechanism (Simulated)
- Status Dashboard (Mock Data)
- Credit Application (Mock)
