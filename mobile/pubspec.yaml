name: customer_app
description: Fintech Credit App with Lock Screen
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  device_info_plus: ^9.0.0
  shared_preferences: ^2.2.0
  intl: ^0.19.0
  url_launcher: ^6.2.0
  geolocator: ^10.1.0
  flutter_contacts: ^1.1.7+1
  permission_handler: ^11.0.1
  photo_manager: ^3.0.0
  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
